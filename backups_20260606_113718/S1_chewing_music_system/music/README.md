Put the loop files for S1 here.

Expected layer names:

- `drum.wav`, `drum.mp3`, or `drum.ogg`
- `background.wav`, `background.mp3`, or `background.ogg`
- `bass.wav`, `bass.mp3`, or `bass.ogg`
- `melody.wav`, `melody.mp3`, or `melody.ogg`

The muffled background sound in the flowchart should be named `background`.
All loaded layers are played in a continuous loop; S1 switches layers by changing volume.
