# Upload Manifest

Upload this folder's contents to WeChat Cloud Storage without renaming files:

- `images/*` -> cloud folder `images/`
- `music/*` -> cloud folder `music/`

After uploading, configure `../wechat_ble_probe/config/cloud-assets.js`.
